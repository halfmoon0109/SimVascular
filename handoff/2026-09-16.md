# 2026-09-16 — macOS_write

## 열린 항목
- [ ] #1 재빌드 검증: 46bfdd2a 이후 12커밋(68953b5a…875d6f27) 전부 미빌드 — 담당: 사용자(Windows/Docker)
- [ ] #2 단독 구멍(지퍼)이 관통하면 대안 닫힘이 없음 — 넓은 구멍 V자 닫기 필요 여부는 #1 결과로 판단 — 담당: Claude
- [ ] #3 미수렴(라운드 한도)을 하드 실패로 둘지, 경고로 낮출지 — 실측에서 멀쩡한 메시를 막으면 재고 — 담당: 사용자
- [ ] #4 컷 점 이분법이 kept에 붙은 삼각형을 각도 무시하고 제외 → 이분법이 non-clear를 못 찾고 u=0.95 클램프될 수 있음. TrimCrossing으로 실제 발생 여부 확인 — 담당: Claude (제기: 09:30 항목)
- [x] #5 rim 강등 → SV_ERROR (68953b5a)
- [x] #6 닫힘 삼각형끼리 교차 → 후보별 관통 수 선택 (e0cd28ff)
- [x] #7~#13 Codex 1차 리뷰 7건 (fd616029…59f8456f)
- [x] #14~#16 Codex 2차 리뷰 3건 (466174c4, 875d6f27)

## 기록

### 09:30 [Claude] 46bfdd2a 정적 리뷰
- 한 일: 직전 커밋(컷 점을 변 위 이분법으로) 정독. `u<edgeFloor`·fold 강등이 cap rim 점도 강등해 다음 라운드 rim 검사에서 즉시 SV_ERROR 나는 경로 발견.
- 커밋: `68953b5a` rim 점은 강등하지 않고 컷 점을 edgeFloor에 붙듦, `numRimHeld` 로그
  `648d1bdc` 두께 0으로만 잘린 점을 컷 합계에 포함
- 검증: 정적(중괄호 균형, `rimOfPoint` 스코프, 헤더 포함)만. 빌드 로그는 7월 것.
- 열어 둠 → #4

### 11:00 [사용자] 3차 재빌드 로그 (648d1bdc)
- 트리밍 2라운드 수렴, rim 강등 0, 접힘 0, 4.9s. 인터페이스 미달 213점(2차 257, wedge 1228).
- 자체검사: seam band 13개가 fragment 관통(hole 16, 20).
- TetGen 실패: segment/facet 교차 (-40.41,-23.43,-99.02), 5점 전부 컷 점.
- 바깥면 AR 1061 sliver, 변 0.50/0.03/0.47.

### 11:20 [Claude] 로그 판독 + 닫힘 선택
- 판독: fragment는 항상 원래 정점을 포함하므로 5점 전부 컷 점인 facet = **닫힘 삼각형끼리 교차**. 209점 구멍 근처인데 로그 8줄 제한에 잘려 안 찍힘. AR 1061은 t/R>1 구역에서 구멍 폭이 0.2~0.5로 넓다는 뜻(얕은 각 교차 → 1.03선이 멀리 밀림). 병목은 트리밍이 아니라 구멍 닫기.
- 커밋: `e0cd28ff` 쌍 구멍은 band 양방향 + 각자 지퍼 3후보 중 관통 최소 선택; TrianglesCross가 꼭짓점 1개 공유 쌍의 반대변도 검사; 구멍별 관통 전부 로그
- 열어 둠 → #2

### 13:00 [Codex] 1차 리뷰 (648d1bdc 기준, 7건)
- #7 [P1] `sv_TetGenMeshObject.cxx:2904` shellBehavior에 neighout 없음, `sv_tetgenmesh_utils.cxx:522` adjtetlist 무조건 읽음 → 체적화 성공 직후 null 역참조
- #8 [P1] `:3002` 바깥벽 ModelFaceID = 벽표면 max+1 → cap id와 충돌(wall=1, inlet=2), exporter가 inlet 파일에 섞음
- #9 [P1] `sv_tetgenmesh_utils.cxx:5270` bin = 경계상자/두께³ → 100³·t=0.05면 54.7 GiB
- #10 [P2] `:4767` 최소면적 DP가 면적 0 삼각형 허용 — (0,0),(2,0),(2,1),(1,1),(0,1)로 재현
- #11 [P2] `:5739` 세 정점이 안전해도 삼각형 안이 벽을 지날 수 있음; 관통 검사가 닫힘 삼각형만 출발점
- #12 [P2] `:5823` 8회 도달해도 SV_OK
- #13 [P2] `MeshingTetGenOptions_PyClass.cxx:1105` .msh 로더가 명령 이름을 map 키로 → localWallThickness 여러 face면 마지막만
- 검증: 호출 경로·내장 TetGen 대조, 독립 계산, `git diff --check`. 빌드 안 함.

### 14:00 [Claude] 1차 리뷰 반영
- 답변 → #7~#13: 7건 전부 코드로 확인, 실제 결함.
- 커밋: `fd616029` #7 neighout=2 + ConvertToVTK 배열 검사
  `1fe5bf52` #8 originalpolydata_ 면 id 전체 위로 (exporter 등록은 별도 → #16으로)
  `8e07b9fa` #9 bin 변을 max(reach, ∛(부피/4M))로, 초과 시 에러, 격자 크기 로그
  `0787df47` #10 퇴화 삼각형에 루프크기² 벌점 — 재현 루프 1/0.5/0.5로 분할 확인
  `fcac674e` #11 #12 전체 삼각형 관통 검사(시트 교차 별도 집계), 수렴/한도 구분, `numUnresolved` out-param → 호출자가 두께 리포트 뒤 SV_ERROR
  `59f8456f` #13 map → 파일 순서 목록 (localSize/sphereRefinement도 같은 버그였음)
- 검증: 정적만.
- #7 소감: TetGen이 성공하는 순간 터졌을 결함 — 지금까지 TetGen 실패 뒤에 숨어 있었음.

### 15:00 [Codex] 2차 리뷰 (3건)
- #14 [P2] `:6581` converged가 numUnresolved에 반영 안 됨 → 8회째 demote 보류돼도 통과
- #15 [P2] `:5911` lastFolded는 접힌 삼각형 수가 아니라 잘라낼 정점 수 — rim이면 numRimHeld만 늘고 접힘 0
- #16 [P2] `sv4gui_MeshLegacyIO.cxx:259` exporter가 모델 face만 순회 → 바깥벽 파일 없음
- 확인: neighout, 격자 상한(402만 bin, 배열당 15.3 MiB), 퇴화 재현(1/0.5/0.5, 단 벌점이라 보장은 아님), 전체 관통 검사, Python 목록.

### 15:30 [Claude] 2차 리뷰 반영
- 답변 → #14 #15: `466174c4` `foldedTriangles`(마지막 라운드 접힌 삼각형 수)를 numUnresolved에; `cutConverged` out-param, 호출자가 미수렴이면 SV_ERROR.
  소견: 보류된 컷은 변의 1/20 자리 sliver라 기하는 성립(TetGen이 거부하지 않음). 요청대로 중단으로 두되 실측에서 멀쩡한 메시를 막으면 재고 → #3
- 답변 → #16: `875d6f27` 표면 ModelFaceID 중 모델 면 식별자에 없는 것을 `mesh-surfaces/wall_outer.vtp`(하나) / `wall_outer_<id>.vtp`(여럿)로 씀.
- 답변 → #10 "보장 아님": 퇴화 없는 분할이 있으면 항상 그쪽, 없을 때만 퇴화 — 그때는 대안이 없어 실패보다 낫다고 봄. 이견 있으면 적어 달라.
- 요청 → Codex: 466174c4, 875d6f27 두 커밋 diff 점검. 특히 `PlyDtaUtils_GetFacePolyData`에 id를 넘길 때 `int` 주소를 쓰는 것과 `ResetFaceSurfaceIds` 적용 순서.
- 요청 → 사용자: 재빌드(#1). 로그에서 `converged`, `N closing triangles and M sheet triangles pass through`, `grid A x B x C bins`, `held N cap rim corners` 확인.
